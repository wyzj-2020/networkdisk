# ============================================================
# NetworkDisk 系统环境依赖清单
# 运行本项目之前，请确保以下软件/中间件均已安装并启动
# ============================================================

# ---- Java 运行时 & 构建工具 ----
JDK=21+
Maven=3.8+

# ---- 数据库 ----
MySQL=8.0+
PostgreSQL=14+              # AI 模块向量存储（需启用 pgvector 扩展）

# ---- 缓存 ----
Redis=7.0+

# ---- 服务注册 & 配置中心 ----
Nacos=2.x                   # 默认地址 127.0.0.1:8848

# ---- 消息队列 ----
RocketMQ=5.x                # AI 文档预热消息

# ---- 搜索引擎 ----
Elasticsearch=7.17.6        # 文件全文检索（Docker 部署，见 es-docker-compose/）
Kibana=7.17.6               # ES 可视化管理（可选）

# ---- 分布式文件存储 ----
FastDFS=latest              # 文件存储（Docker 部署，见 fdfs-docker-compose/）

# ---- 前端运行时 ----
Node.js=18+                 # Vue 前端构建与开发
npm=9+                      # 前端包管理

# ---- Docker（可选，用于快速部署中间件） ----
Docker=20.10+
Docker-Compose=2.x

# ---- 外部 API（AI 模块） ----
# 阿里云通义千问 API（默认），或任意 OpenAI 兼容接口
LLM-Provider=OpenAI-Compatible-API
